# Use Cases

The following use cases describe how the {{ entity.name }} entity is used within the domain.

{% for behavior in entity.behaviors %}

## {{ behavior }}

The {{ behavior }} operation modifies or interacts with the {{ entity.name }} entity.

{% endfor %}

