# RFC-0003 — Plugin Discovery & Distribution

# 18 — Migration

This document defines migration considerations.

The existing Plugin SDK v2 must remain compatible.

Migration principles:

- no unnecessary breaking changes;
- explicit compatibility rules;
- gradual adoption.

Existing internal plugins should be migrated progressively to the distributed package model.
