# RFC-0003 — Plugin Discovery & Distribution

# 09 — Plugin Manifest

The plugin manifest is the official description of a FamilyOS plugin.

The manifest provides:

- identity;
- metadata;
- version;
- compatibility;
- entrypoint;
- dependencies;
- capabilities.

The default manifest file is:

plugin.toml

The manifest must be versioned and validated before installation.
